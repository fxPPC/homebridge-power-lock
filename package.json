{
  "name": "homebridge-power-lock",
  "version": "0.5.0",
  "description": "A Homebridge plugin that creates a virtual lock mechanism in MQTT, Linux command line, or dummy mode.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/fxPPC/homebridge-power-lock.git"
  },
  "bugs": {
    "url": "https://github.com/fxPPC/homebridge-power-lock/issues"
  },
  "engines": {
    "node": ">=16.0.0",
    "homebridge": ">=1.6.0"
  },
  "keywords": [
    "homebridge-plugin",
    "homebridge",
    "homekit",
    "mqtt",
    "lock",
    "linux",
    "cli",
    "virtual"
  ],
  "author": "Alice aka fxPPC",
  "main": "dist/index.js",
  "scripts": {
    "build": "mkdir -p dist && cp -r src/* dist/",
    "lint": "echo \"(Optional) Lint step could be placed here.\"",
    "test": "echo \"No tests specified\""
  },
  "devDependencies": {},
  "dependencies": {
    "mqtt": "^4.3.7"
  }
}
